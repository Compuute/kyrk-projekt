name: terraform-plan

# GitOps review gate. On any PR that touches infra/terraform, show the plan for
# each environment so reviewers see the exact diff before merge. Read-only —
# never applies. Pairs with terraform-apply.yml (which applies after merge).
on:
  pull_request:
    paths:
      - 'infra/terraform/**'

# Collapse rapid pushes on the same PR to the latest plan.
concurrency:
  group: terraform-plan-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  plan:
    name: plan (${{ matrix.environment }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        environment: [dev, prod]
    environment: ${{ matrix.environment }}
    defaults:
      run:
        working-directory: infra/terraform
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP via WIF
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: sa-terraform@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com

      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.7.5"

      - name: Generate tfvars from environment secrets
        run: |
          cat > terraform.tfvars <<TFVARS
          project_id        = "${{ secrets.GCP_PROJECT_ID }}"
          region            = "${{ secrets.GCP_REGION }}"
          environment       = "${{ matrix.environment }}"
          github_repository = "${{ github.repository }}"
          # Firestore location policy: prod = eur3 (multi-region), dev = regional.
          # Immutable after database creation — a change here makes the plan
          # show destroy+recreate; export/import the data before applying.
          database_location = "${{ matrix.environment == 'prod' && 'eur3' || secrets.GCP_REGION }}"
          TFVARS

      - name: terraform init + select workspace
        run: |
          terraform init -input=false
          terraform workspace select ${{ matrix.environment }}

      - name: terraform plan
        run: |
          set -o pipefail
          echo "## Terraform plan — ${{ matrix.environment }}" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          terraform plan -no-color -input=false 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
